# Learn more about services, parameters and containers at
# https://symfony.com/doc/current/service_container.html
parameters:
    #parameter_name: value

services:
    # default configuration for services in *this* file
    _defaults:
        # automatically injects dependencies in your services
        autowire: true
        # automatically registers your services as commands, event subscribers, etc.
        autoconfigure: true
        # this means you cannot fetch services directly from the container via $container->get()
        # if you need to do this, you can override this setting on individual services
        public: false

    # makes classes in src/AppBundle available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    AppBundle\:
        resource: '../../src/AppBundle/*'
        # you can exclude directories or files
        # but if a service is unused, it's removed anyway
        exclude: '../../src/AppBundle/{Entity,Repository,Tests}'

    # controllers are imported separately to make sure they're public
    # and have a tag that allows actions to type-hint services
    AppBundle\Controller\:
        resource: '../../src/AppBundle/Controller'
        public: true
        tags: ['controller.service_arguments']

    twig.extension.text:
        class: Twig\Extensions\TextExtension
        tags:
            - { name: twig.extension }

    twig.extension.intl:
        class: Twig\Extensions\IntlExtension
        tags:
            - { name: twig.extension }

    AppBundle\Services\Auth0\Auth0Manager:
        arguments:
            $auth0Data:
                domain: "%auth0.base_url%"
                clientId: "%auth0.api.client_id%"
                clientSecret: "%auth0.api.client_secret%"
                loginClientId: "%auth0.login.client_id%"
                auth0EmailTtl: "%auth0.api.ttl_sec%"

    AppBundle\Services\SendBird\SendBirdManager:
        arguments:
            $sendbirdParams:
                appId: "%sendbird.app_id%"
                apiUrl: "%sendbird.api_url%"
                apiToken: "%sendbird.api_token%"

    AppBundle\Services\Slack\SlackManager:
        arguments:
            $slackParams:
                apiUrl: "%slack.api_url%"
                apiToken: "%slack.api_token%"
                admins: "%slack.admins%"

    AppBundle\Services\Geocoding:
        arguments:
            $apiKey: "%osm.geocoding_api%"
