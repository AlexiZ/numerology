{% extends 'base.html.twig' %}

{% block title %}
    Numérologie - {{ subject }}
{% endblock %}

{% block stylesheets %}
    {{ parent() }}

    <link rel="stylesheet" type="text/css" href="{{ asset('bundles/app/css/print.css') }}" media="print">
{% endblock %}

{% block body %}
    <div class="container">
        <h1>Numérologie <span class="subject">{{ subject }}</span></h1>

        <ol class="breadcrumb">
            <li><a href="{{ path('numerologie_index') }}">Accueil</a></li>
            <li class="active">{{ subject }}</li>
        </ol>

        <div class="panel with-nav-tabs">
            <div class="panel-heading">
                <ul class="nav nav-tabs">
                    <li class="active"><a href="#tab1" data-toggle="tab">Résumé identité</a></li>
                    <li class=""><a href="#tab2" data-toggle="tab">Calcul identité</a></li>
                    <li class=""><a href="#tab3" data-toggle="tab">Résumé lettres</a></li>
                    <li class=""><a href="#tab4" data-toggle="tab">Calcul lettres</a></li>
                    <li class=""><a href="#tab5" data-toggle="tab">Vibrations de soutien</a></li>
                </ul>
            </div>

            <div class="panel-body">
                <div class="tab-content">
                    <div class="tab-pane fade active in" id="tab1">
                        {% include '@App/Numerologie/_resume_identite.html.twig' with {'subject': subject} %}
                    </div>

                    <div class="tab-pane fade" id="tab2">
                        {% include '@App/Numerologie/_identite.html.twig' with {'subject': subject} %}
                    </div>

                    <div class="tab-pane fade" id="tab3">
                        {% include '@App/Numerologie/_resume_lettres.html.twig' with {'subject': subject} %}
                    </div>

                    <div class="tab-pane fade" id="tab4">
                        {% include '@App/Numerologie/_lettres.html.twig' with {'subject': subject} %}
                    </div>

                    <div class="tab-pane fade" id="tab5">
                        {% include '@App/Numerologie/_vibrations.html.twig' with {'subject': subject} %}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}