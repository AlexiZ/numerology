{% extends 'base.html.twig' %}

{% block title %}
    Numérologie - Ajouter
{% endblock %}

{% block bodyAttr %}data-tutorial="numerologie_add"{% endblock bodyAttr %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold text-primary">
                        Ajouter une nouvelle personne
                    </h6>
                </div>

                <div class="card-body">
                    {% form_theme form '@Extranet/Numerologie/_form_theme.html.twig' %}

                    {{ form_start(form) }}
                    {{ form_errors(form) }}
                    {{ form_rest(form) }}

                    <button type="submit" id="submitAdd" class="btn btn-success btn-icon-split">
                        <span class="icon text-white-50"><i class="fas fa-check"></i></span>
                        <span class="text">Voir les résultats</span>
                    </button>

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
