<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <style>
            #logo {
                height: 45px;
            }
            .container {
                margin: auto;
                padding-bottom: 10px;
            }
            .one {
                width: 15%;
                float: left;
            }
            .two {
                margin-left: 15%;
                height: 50px;
            }
            .two p {
                padding: 0;
                margin: inherit;
                padding-top: 15px;
                color: #858796;
            }
        </style>
    </head>

    <body>
        <header class="container">
            <div class="one">
                <img src="{{ path ~ asset('bundles/site/img/Logo-photoshop.png') }}" alt="Logo" id="logo" />
            </div>

            <div class="two">
                <p>Numérologie {{ subject }} - {{ "now"|date("d-m-Y") }}</p>
            </div>
        </header>
    </body>
</html>